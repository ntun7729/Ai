{
  "name": "ai-website-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1",
    "dev:open": "wrangler dev --ip 0.0.0.0",
    "dev:local": "node scripts/dev-local.mjs",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
